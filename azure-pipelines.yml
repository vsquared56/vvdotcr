trigger:
- main
- develop
- feature/*

pool:
  name: linux

variables:
- name: nodeVersion
  value: 18

stages:
- stage: Dev
  variables:
  - group: azure_deployment_dev
  jobs:
  - job: DeployDev
    steps:
    - template: azure-pipelines-deploy.yml