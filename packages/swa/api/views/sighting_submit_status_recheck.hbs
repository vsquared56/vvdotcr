<div class="sighting-submit" hx-get="/api/submission-status?submissionId={{submissionId}}&recheckCount={{recheckCount}}" hx-trigger="every {{recheckInterval}}s" hx-swap="outerHTML">
  <div class="sighting-submission-id">{{submissionId}}</div>
    Image saved... rechecking status every {{recheckInterval}} seconds...
  <div class="sighting-submission-status">
    {{#if pendingResizing}}
    Current status: pending resizing.
    {{else if pendingAutomaticApproval}}
    Current status: pending automatic approval.
    {{/if}}
  </div>
</div>