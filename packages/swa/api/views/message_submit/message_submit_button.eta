<button id="message-submit-button"
        class="btn btn-primary min-w-[304px] h-[69px]"
        <% if (!it.submitEnabled) { %>
        disabled="disabled"
        <% } %>
        <% if (it.replace) { %>
        hx-swap-oob="true"
        <% } %>
        >
  <% if (it.submitEnabled) { %>
  Send <%= it.numMessages %> message<% if (it.numMessages > 1) { %>s<% } %>!
  <% } else { %>
  Pick something to send above!
  <% } %>
</button>